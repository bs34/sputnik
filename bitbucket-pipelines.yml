image: otormaigh/android-pipelines

pipelines:
  branches:
    develop:
      - step:
          script:
            - ./envar.sh
            - ./gradlew clean build uploadArchives
    snapshot-*:
      - step:
          script:
            - ./envar.sh
            - ./gradlew clean build uploadArchives